namespace SpaceWarpModLibrary.PreloadPatcher;

/// <summary>
/// Preload patcher for SpaceWarpModLibrary.
/// </summary>
public static class Patcher
{
    // If you do not need a preload patcher, remove this project and the reference to it in your main project file.
    // If you do need a preload patcher, open the Directory.Build.targets file in the main SpaceWarpModLibrary project
    // (SpaceWarpModLibrary/src/SpaceWarpModLibrary/Directory.Build.targets) and follow the instructions in comments
    // to enable its building.
}